


void Motormovement (int sensordistance, int sensorpin){
    if (sensorpin == sensorUP){
        if (sensorditance > xx){ //tests if the sensor distance is greater than the noise value

            int time = xx;  // amount of time the motors are running for

            int motorpin1 = xx; // setting the motorpins to match the motors which need to be activated
            int motorping2 = xx;

            Motormove (motorpin1, motorpin2, int PWMpercentage, time)  //calling motor function for motors to run
        }
    }
    else if (sensorpin == sensorDOWN){
        if (sensorditance > xx){ //tests if the sensor distance is greater than the noise value

            int time = xx;  // amount of time the motors are running for

            int motorpin1 = xx; // setting the motorpins to match the motors which need to be activated
            int motorping2 = xx;

            Motormove (motorpin1, motorpin2, int PWMpercentage, time)  //calling motor function for motors to run
        }
    }
    else if (sensorpin == sensorLEFT){
        if (sensorditance > xx){ //tests if the sensor distance is greater than the noise value

            int time = xx;  // amount of time the motors are running for

            int motorpin1 = xx; // setting the motorpins to match the motors which need to be activated
            int motorping2 = xx;

            Motormove (motorpin1, motorpin2, int PWMpercentage, time)  //calling motor function for motors to run
        }
    }
    else if (sensorpin == sensorRIGHT){
        if (sensorditance > xx){ //tests if the sensor distance is greater than the noise value

            int time = xx;  // amount of time the motors are running for

            int motorpin1 = xx; // setting the motorpins to match the motors which need to be activated
            int motorping2 = xx;

            Motormove (motorpin1, motorpin2, int PWMpercentage, time)  //calling motor function for motors to run
        }
    }
}
